import 'dotenv/config';

const {
  BOT_TOKEN,
  CHANNEL_ID,
  BOT_ID,
  BASIC_ROLE_ID,
  STANDARD_ROLE_ID,
  PREMIUM_ROLE_ID
} = process.env;

export {
  BOT_TOKEN,
  BOT_ID,
  CHANNEL_ID,
  BASIC_ROLE_ID,
  STANDARD_ROLE_ID,
  PREMIUM_ROLE_ID
};
