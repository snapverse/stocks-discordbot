import 'dotenv/config';

const {
  API_PREFIX,
  BOT_TOKEN,
  CHANNEL_ID,
  BOT_ID,
  BASIC_ROLE_ID,
  STANDARD_ROLE_ID,
  PREMIUM_ROLE_ID,
  SERVER_ID
} = process.env;

export {
  API_PREFIX,
  BASIC_ROLE_ID,
  BOT_ID,
  BOT_TOKEN,
  CHANNEL_ID,
  PREMIUM_ROLE_ID,
  STANDARD_ROLE_ID,
  SERVER_ID
};
