{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "buildBefore",
      "type": "shell",
      "command": "yarn",
      "args": [
        "run",
        "build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
